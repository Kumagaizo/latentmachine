[package]
name = "latent_cli"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

[[bin]]
name = "latent"
path = "src/main.rs"
